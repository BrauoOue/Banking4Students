version: "3.8"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    # depends_on:
    #   - db
    networks:
      - app-network

  # react-native:
  #   build: ./BankingForStudents
  #   container_name: frontend
  #   ports:
  #     - "8081:8081"
  #     - "19000:19000"
  #     - "19001:19001"
  #     - "19002:19002"
  #   volumes:
  #     - ./BankingForStudents:/app
  #   depends_on:
  #     - backend
  #   networks:
  #     - app-network

  # db:
  #     image: postgres:15-alpine
  #     ports:
  #       - "5433:5432"
  #     environment:
  #       POSTGRES_DB: Banking
  #       POSTGRES_USER: postgres
  #       POSTGRES_PASSWORD: banking123
  #     volumes:
  #       - postgres_data:/var/lib/postgresql/data
  #     networks:
  #       - app-network


# volumes:
#   postgres_data:

networks:
  app-network:
    driver: bridge